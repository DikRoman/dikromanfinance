{
  "name": "selfpage_financespec",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "bash scripts/build.sh",
    "serve": "npm run build && cd dist && python3 -m http.server 8000",
    "start": "npm run serve",
    "vercel-build": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fontsource/montserrat": "^5.2.8",
    "aos": "^2.3.4"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
